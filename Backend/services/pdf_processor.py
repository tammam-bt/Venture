def extract_text(file_stream):
    """
    MOCK FUNCTION: Pretends to extract text from a PDF file stream.
    Expected signature: extract_text(file_stream) -> dict
    """
    return {
        "text": "Mock extracted text: This startup aims to use AI to revolutionize the space industry with an expected ROI of 200%.",
        "status": "success"
    }
