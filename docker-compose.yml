services:
  backend:
    build: 
      context: ./Backend
      dockerfile: Dockerfile
    container_name: venture_backend
    restart: always
    ports:
      - "5000:5000"
    env_file:
      - ./Backend/.env
    # Note: Database is hosted remotely on Supabase, so no postgres service is needed locally
